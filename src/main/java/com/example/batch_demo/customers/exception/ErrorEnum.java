package com.example.batch_demo.customers.exception;

public enum ErrorEnum {
    INVALID_JOB_PARAMETERS,
    BATCH_JOB_CONFLICT,
    JOB_NOT_FOUND,
    BATCH_JOB_ERROR,
    JOB_ALREADY_RUNNING,
    JOB_LAUNCH_ERROR
}
