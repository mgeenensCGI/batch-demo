post:
  tags:
    - Customer Report
  summary: Export a customer report
  description: This endpoint triggers the export of a customer report and returns.
  responses:
    '202':
      description: Job launch accepted
      content:
        application/json:
          schema:
            $ref: '../schemas/job-run-response.yaml'
    '400':
      description: Invalid job parameters
      content:
        application/json:
          schema:
            $ref: '../schemas/batch-error-response.yaml'
    '409':
      description: Job already running or cannot be restarted
      content:
        application/json:
          schema:
            $ref: '../schemas/batch-error-response.yaml'
    '500':
      description: Unexpected error
      content:
        application/json:
          schema:
            $ref: '../schemas/batch-error-response.yaml'